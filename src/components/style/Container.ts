import styled from "styled-components";
import { FULL } from "./sizes";

export const ContainerWrapper = styled.div`
  margin: 0;
  display: flex;
  width: ${FULL};
  flex-direction: column;
  align-items: center;
  box-sizing: border-box;
`;
